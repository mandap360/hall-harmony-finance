// PartySection is no longer used; AccountsPage now uses AccountSection for parties too.
// Kept as a thin re-export for backward compatibility.
export { AccountSection as PartySection } from './AccountSection';
